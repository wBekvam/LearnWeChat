<view class="page-section">
  <view>
    <form bindsubmit="formSubmit">
      <view class="book_input">
        <input class="weui-input" type="number" maxlength="12" bindblur="bindInput" name='studentID' placeholder="请输入您的学号" />
      </view>
      <view>
        <button class="weui-button" form-type="submit" type="{{buttonType}}" size="{{primarySize}}" 
        loading="{{loading}}" plain="{{plain}}" disabled="{{buttonStatus}}" bindquerybooks="bindQueryBooks">预约查询 </button>
      </view>
    </form> 
  </view>
</view>